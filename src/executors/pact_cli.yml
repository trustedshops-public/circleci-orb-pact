description: >
  Pact CLI
docker:
  - image: 'pactfoundation/pact-cli:<<parameters.tag>>'
parameters:
  tag:
    # renovate: datasource=docker depName=pactfoundation/pact-cli versioning=regex:^(?<major>\d+)\.(?<minor>\d+)\.(?<patch>\d+)\.(?<build>\d+)$
    default: "1.5.0.3"
    description: >
      Pick a specific image variant:
      https://hub.docker.com/r/pactfoundation/pact-cli/tags
    type: string
