description: >
  Pact CLI
docker:
  - image: 'pactfoundation/pact-cli:<<parameters.tag>>'
parameters:
  tag:
    # renovate: datasource=docker depName=pactfoundation/pact-cli versioning=docker
    default: 0.51.0.0
    description: >
      Pick a specific image variant:
      https://hub.docker.com/r/pactfoundation/pact-cli/tags
    type: string
